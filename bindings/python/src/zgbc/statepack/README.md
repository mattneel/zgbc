Bundled save states for Pokegym.

Each ``.state`` file contains the raw bytes returned by ``GameBoy.save_state()``.
Copy them into the pokegym ``States/`` directory via ``zgbc.pokegym.ensure_state_pack()``.
